@using Microsoft.EntityFrameworkCore;

@model HTTPMediaPlayerCore.Models.Lesson

@if (Model != null)
    {
        var text = Model.Text;
        if (text != null)
        {
            @Html.Raw(text)
        }
    }
